/*
Write an interactive program that contains an if statement that may be used to 
compute the area of square (area=side2) or a circle (area=Π X radius2) after 
prompting the user to type the first character of the figure names (S or C).
*/
#include<stdio.h>
int main()
{
    char ui;
	printf("Enter S for calculating Square or C for calculating Circle\n");
    scanf("%c",&ui);
    
    switch (ui)
    {
    case 'S':
        
        break;
    case 'C':
    

    
    default:
        break;
    }
    
}